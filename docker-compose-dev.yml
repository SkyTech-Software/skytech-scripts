version: "3"
services:
  api:
    image: skytech-scripts:local
    container_name: skytech-scripts
    build:
      context: .
      dockerfile: Dockerfile
      target: development
    volumes:
      - ./backend:/app/backend
    command: ["uvicorn", "backend.main:app", "--host", "0.0.0.0", "--port", "80", "--reload",  "--reload-dir", "/app"]
    env_file:
      - .env
    ports:
      - "8020:80"
